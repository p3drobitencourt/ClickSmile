spring.jpa.hibernate.ddl-auto=update

# In Docker profile we still run Flyway; allow Hibernate to reconcile minor schema
# differences during local smoke testing. Remove or set to 'validate' for prod.
